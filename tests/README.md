# Tests
La struttura di questa cartella è identica a quella del codice sorgente. Si riporta comunque la struttura per la presenza di eventuali file aggiuntivi
```
tests/
|
|--- controller/ #gestione dell'interazione del Giocatore (tests)
|
|--- model/ #gestione di logica di gioco e dati (tests)
|
|--- view/ #gestione della UI (tests)
```